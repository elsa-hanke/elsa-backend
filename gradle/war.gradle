apply plugin: "war"

bootWar {
    mainClassName = "fi.oulu.elsa.ElsaBackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
